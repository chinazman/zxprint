"use strict";

/**
 * 打印元素对象
 */

class PrintElementEntity {
  constructor(tid, options, printElementType) {
    this.tid = tid;
    this.options = options;
    this.printElementType = printElementType;
  }
}
export default PrintElementEntity;
